[project]
name = "gameprice-kr-backend"
version = "0.1.0"
description = "Canonical price ingestion and public read API for GamePrice KR"
requires-python = "==3.12.*"
dependencies = [
    "django==5.2.17",
    "psycopg[binary]==3.3.4",
]

[tool.uv]
package = false
